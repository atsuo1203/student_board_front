import styled from 'styled-components';

export const reloadButtonStyle = {
  position: 'relative',
  top: '-10px',
}

export const createButtonStyle = {
  button: {
    position: 'relative',
    top: '-2px',
    left: '-20px',
    height: '30px',
    transform: 'scale(0.7)',
  },
  buttonText: {
    position: 'relative',
    top: '2px',
  }
}